@model Sanitario.Models.DettagliVendita

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>DettagliVendita</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdVendita" class="control-label"></label>
                <select asp-for="IdVendita" class ="form-control" asp-items="ViewBag.IdVendita"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdProdotto" class="control-label"></label>
                <select asp-for="IdProdotto" class ="form-control" asp-items="ViewBag.IdProdotto"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
