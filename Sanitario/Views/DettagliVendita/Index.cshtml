@model IEnumerable<Sanitario.Models.DettagliVendita>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vendita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prodotto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vendita.IdVendita)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prodotto.Descrizione)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDettagliVendita">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDettagliVendita">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDettagliVendita">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
