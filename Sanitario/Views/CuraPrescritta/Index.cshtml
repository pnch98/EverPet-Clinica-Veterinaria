@model IEnumerable<Sanitario.Models.CuraPrescritta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdProdotto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Visita)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdProdotto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Visita.Esame)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCuraPrescritta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCuraPrescritta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCuraPrescritta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
